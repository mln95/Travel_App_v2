import {handleSubmit_first} from '../src/client/js/handleSubmit_first'

describe("Testing the submit functionality", () => {  
  test("Testing the handleSubmit() function", () => {
         expect(handleSubmit_first).toBeDefined();
})});